# 美发宝 — 使用说明书

---

## 一、安装与激活

### 安装
- **Windows**：双击 `美发宝_Setup.exe`，按提示安装
- **macOS**：双击 `.dmg` 文件，拖入「应用程序」文件夹
  - 首次打开时如果提示"无法验证开发者"，右键点击 → 选「打开」即可

### 激活
1. 打开软件，看到激活页面，显示**12位机器码**（如 `E6854C20E283`）
2. 将机器码发给卖家，获取对应的**12位激活码**
3. 输入激活码，点击「激活」
4. 一机一码，永久有效

---

## 二、快速上手

### 1. 改店名
进入「**设置**」→ 输入你的店名 → 点「💾 保存」

### 2. 添加服务项目
进入「**服务**」→ 点击「新增服务」→ 填写名称、价格、分类

> 系统默认已有：剪发、洗吹、染发、烫发、护理、造型

### 3. 添加会员
进入「**会员**」→ 点击「新增会员」→ 填写姓名、手机号、等级、余额

> 如果有大量老会员数据，使用下方「Excel 导入」功能（详见第三部分）

### 4. 收银结账
进入「**收银**」→ 搜索会员 → 点击服务项目 → 选择支付方式 → 确认结账
> 结账成功后会语音播报消费金额和余额（可在设置关闭）

---

## 三、Excel 批量导入会员

### 方式一：使用模板（推荐）

1. 打开项目中的 `会员导入模板.xlsx`
2. 按照模板格式填写你的老会员数据：
   - **姓名**、**手机号**为必填，不能为空
   - **等级**：普通/银卡/金卡/钻石，不填默认为「普通」
   - **余额**：会员当前储值余额（元）
   - **累计消费**：历史累计消费金额（元），用于自动升级
   - **备注**：选填，任意说明
3. 保存后进入软件「**设置**」→ 「📤 导入 Excel 会员」
4. 选择文件 → 系统自动匹配列 → 预览确认 → 确认导入

### 方式二：自行准备 Excel

只要表头包含以下关键词，系统会自动匹配：

| 数据 | 可匹配的表头关键词 |
|------|-------------------|
| 姓名 | 姓名、名字、会员名、name |
| 手机号 | 电话、手机、号码、phone |
| 等级 | 等级、级别、level |
| 余额 | 余额、储值、balance |
| 累计消费 | 累计、消费、总额、total |
| 备注 | 备注、说明、note |

> 💡 如果自动匹配不对，可以在映射步骤手动选择对应列

### 导入注意事项

- 姓名或手机号为空的**行会被自动跳过**，系统会提示跳过数量
- 相同手机号的会员会被跳过（不会覆盖已有数据）
- 导入完成后回到「会员」页面即可查看

---

## 四、功能详解

### 收银页

| 功能 | 操作 |
|------|------|
| 搜索会员 | 输入手机号或姓名 → 回车/点搜索 |
| 选择服务 | 搜索到会员后，点左侧服务按钮加入清单 |
| 自定义金额 | 左侧底部「✚ 其他收费」→ 输入名称+金额 → 确认 |
| 支付方式 | 💵 现金 / 💳 余额 |
| 确认结账 | 点底部绿色按钮，语音播报结果 |

> 余额支付：自动从会员余额中扣款，余额不足会提示

### 会员管理

| 功能 | 操作 |
|------|------|
| 新增会员 | 左上角「新增会员」按钮 |
| 编辑会员 | 点击会员卡片 → 修改 → 保存 |
| 删除会员 | 点击会员卡片 → 删除按钮 |
| 充值 | 点击会员卡片 → 充值 |
| Excel导入 | 详见第三部分 |

### 会员等级自动升级

系统根据累计消费金额自动升级：

| 等级 | 折扣 | 升级门槛 |
|------|------|----------|
| 普通 | 无折扣 | 0元 |
| 银卡 | 9折 | 500元 |
| 金卡 | 8.5折 | 1000元 |
| 钻石 | 7.5折 | 3000元 |

> 折扣率和门槛可在「设置」里修改

### 消费记录

- 查看所有消费/充值记录
- 可按会员筛选
- 支持删除错误记录

### 数据统计

- 今日收入 / 客数 / 充值（收银页首页即显示）
- 「统计」页面：会员等级分布、消费趋势等
- 「收银」页首页的今日概览卡片实时更新

### 系统设置

| 设置项 | 说明 |
|------|------|
| 店铺名称 | 显示在侧边栏和标题栏 |
| 字体大小 | 小/中/大三档，即时生效 |
| 语音播报 | 结账后是否语音播报 |
| 会员等级折扣 | 修改各等级折扣率和升级门槛 |
| 数据管理 | 导出/清空/导入数据 |
| 自动备份 | 每日自动备份为 Excel |

---

## 五、数据备份

系统**每天自动备份**会员数据为 Excel 文件，存储在备份目录。

- 默认备份目录：系统自动生成的 `backups` 文件夹
- 可进入「设置 → 自动备份」自定义目录和保留天数
- 点击「📦 立即备份」可手动触发
- 备份文件格式：`members_2026-05-08.xlsx`

> 💡 建议定期把备份文件拷贝到 U 盘或微信收藏，防止电脑故障丢数据

---

## 六、常见问题

**Q：软件需要联网吗？**
A：不需要，完全本地运行，数据存你电脑上。

**Q：换电脑了怎么办？**
A：用备份功能导出数据，新电脑导入即可。重新激活需联系卖家获取新激活码。

**Q：数据存在哪里？**
A：Windows：`%APPDATA%\美发宝\meifabao.db`；macOS：`~/Library/Application Support/美发宝/meifabao.db`

**Q：软件收费吗？**
A：一次性购买，永久使用，免费更新。

**Q：能同时装多台电脑吗？**
A：标准版授权一台电脑。如需多台请联系卖家购买额外授权。

---

## 七、联系方式

如有问题或建议，请联系卖家。

---

*版本：v0.1.0 | 更新日期：2026年5月*
